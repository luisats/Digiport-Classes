// 1. Simple object
// Make an object 'cat' with 'name', 'color', and 'age'.
// Show the values in the console.

let cat = {
    name: "Oliver",
    color: "white",
    age: 6,
};

console.log("Name: " + cat.name + "\nColor: " + cat.color + "\nAge: " + cat.age);