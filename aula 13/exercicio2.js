// 2. Add and change
// Add 'toy' to the cat object.
// Change the 'age' to another number.

let cat = {
    name: "Oliver",
    color: "white",
    age: 6,
};

cat.toy = "ball";

cat.age = 7;

console.log(cat);